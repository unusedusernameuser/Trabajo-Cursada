# Binarios compilados de Go
/bin/
/dist/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Salida de `go build` cuando se compila el binario del proyecto en la raíz
/my_sqlc_project

# Archivos de test
*.test
*.out
coverage.txt
coverage.html

# Cachés y dependencias
go.work
go.work.sum

# Variables de entorno / secretos
# (el .env de ejemplo con credenciales locales de Docker sí se versiona;
#  si tenés un .env.local con datos propios, no lo subas)
.env.local
.env.*.local

# Docker
# Los datos del volumen de Postgres nunca deberían versionarse
db_data/

# Editor / SO
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
